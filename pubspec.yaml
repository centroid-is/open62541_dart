name: open62541
description: OPC UA stack, bindings to the open62541 library.
version: 1.5.6-1
repository: https://github.com/centroid-is/open62541_dart

environment:
  sdk: ^3.9.0
dependencies:
  archive: ^4.0.7
  binarize: ^2.0.0
  code_assets: ^1.0.0
  collection: ^1.19.0
  ffi: ^2.1.2
  hooks: ">=1.0.0 <3.0.0"
  http: ^1.6.0
  logging: ^1.3.0
  native_toolchain_cmake: ">=0.2.0 <0.4.0"
  tuple: ^2.0.2
platforms:
  linux:
  windows:
  macos:
  android:
  ios:
topics:
  - network
  - industrial
  - opcua
  - open62541
  - hmi
dev_dependencies:
  ffigen: ^20.1.1
  lints: ^6.0.0
  test: ^1.24.0
  import_sorter: ^4.6.0
import_sorter:
  emojis: false
  comments: false
  ignored_files: 
    - open62541.g.dart